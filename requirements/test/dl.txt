# sglang DLIN (DLIN) test dependencies.
# Installed inside the runtime container before the smoke suite.
# DLIN framework wheels (torch/triton/torchvision/flash_attn/xformers) and the
# built sglang wheel are installed from scripts/run/config_env.json (--no-deps),
# so torch's own pure-Python deps MUST be listed here (they are not pulled by
# the --no-deps framework install). Versions pinned to match vLLM's DL lockfile.
typing_extensions==4.15.0
sympy==1.14.0
networkx==3.6.1
jinja2==3.1.6
fsspec==2025.3.0
filelock==3.29.0
mpmath==1.3.0
markupsafe==3.0.3
numpy==2.3.5
# test runner
pytest
pytest-xdist
pytest-shard
tabulate              # required by test/run_suite.py (sglang CI suite runner)
einops==0.8.2
pyyaml==6.0.3
tqdm==4.67.3
